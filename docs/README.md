# docs / 문서 이미지

Screenshots used by the main [README](../README.md). 메인 README에서 쓰는 스크린샷.

Current files / 현재 파일:

- `overwatch-workshop-inspector-settings.png` — Overwatch **Settings → Gameplay →
  General → Custom Games - Workshop** with *Enable Workshop Inspector* and *Enable
  Workshop Inspector Log File* turned on. / 워크숍 인스펙터·로그 파일 활성화 설정 화면.
- `banpick-duel.png` — real-time ban/pick duel board (Team A / Team B). / 밴픽 대전 보드.
- `session-calendar.png` — scrim session calendar view. / 스크림 세션 달력.

> ℹ️ The overview / map-analysis / ultimate-analysis screenshots were removed because
> they showed real aggregate results (win/loss totals, per-map win rates, a real date
> range). They will be re-captured from **synthetic sample data** and added back later.
> ℹ️ 전체 통계 / 맵 분석 / 궁극기 분석 스크린샷은 실제 집계 결과(승·패 합계, 맵별 승률,
> 실제 기간)가 노출되어 제거했습니다. 추후 **합성 샘플 데이터** 기반으로 다시 촬영해 추가할 예정입니다.

To add more / 추가하려면: capture from a **sanitized build** (base team `"Team A"`, no
internal-team branding) using synthetic or anonymized data, drop the PNG here, and
reference it from the README.
**정리된 빌드**(기준 팀 `"Team A"`, 내부 팀 브랜딩 없음)에서 합성·익명 데이터로 촬영해 이
폴더에 넣고 README에서 참조하세요.

> ⚠️ Any unedited internal-build screenshots (that still show the internal team's
> branding or real data) must NOT be published. Keep them out of the repo — the
> `docs/_internal_do_not_publish/` path is **git-ignored** as a safety net; delete such
> files or recapture from a sanitized build.
> ⚠️ 내부 빌드 원본 스크린샷(내부 팀 브랜딩·실데이터가 남은 것)은 **공개 금지**입니다.
> 저장소에 넣지 마세요. `docs/_internal_do_not_publish/` 경로는 안전장치로 **git 무시**
> 처리되어 있습니다 — 해당 파일은 삭제하거나 정리된 빌드에서 재촬영하세요.
